# API internal port for k8s (normally 6443)
k8s_internal_api_port: 6443
# API external port for k8s (accessible through the loadbalancer). define as eg. 7443 if you want to run loadbalancer and master on the same host
k8s_loadbalancer_api_port: 7443